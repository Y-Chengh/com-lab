stmt_if(bool)stmt → ε|else stmt
loc → id loc'
bool → join bool'
parameter' → WithComma parameter'|ε
rel_expr → <= expr|> expr|ε|>= expr|< expr
program → function function | ε
unary → ! unary | factor
type → basic type'
stmts' → stmt stmts'|ε
param → type id
loc' → [ bool ] loc'|ε
expr' → + term expr'|- term expr'|ε
delc → type id ;
function → basic id ( parameters ) block | ε
parameter → param parameter'
rel → expr rel_expr
block → { decls stmts }
expr → term expr'
term → unary term'
join → equality join'
factor → ( bool ) | loc | num | real | true | false
decls' → delc decls'|ε
equality → rel equality'
stmts → stmts'
parameterWithComma → , type id
bool' → or join bool'|ε
term' → * unary term'|/ unary term'|ε
equality' → == rel equality'|!= rel equality'|ε
join' → and equality join'|ε
type' → [ num ] type'|ε
decls → decls'
basic → int | char | float | String
parameters → parameter | ε
stmt → loc = bool ;|return;|break ;|block|do stmt while ( bool ) ;|while ( bool ) stmt|if ( bool ) stmt stmt_if(bool)stmt
