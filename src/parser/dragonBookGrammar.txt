stmt_if(bool)stmt → | else stmt
loc → id loc'
bool → join bool'
rel_expr →  <= expr| > expr|| >= expr| < expr
program → block
unary → ! unary | factor
type → basic type'
stmts' → stmt stmts'|ε
loc' → [ bool ] loc'|ε
expr' → + term expr'|- term expr'|ε
delc → type id ;
rel → expr rel_expr
block → { decls stmts }
expr → term expr'
term → unary term'
join → equality join'
factor → ( bool ) | loc | num | real | true | false
decls' → delc decls'|ε
equality → rel equality'
stmts → stmts'
bool' → or join bool'|ε
term' → * unary term'|/ unary term'|ε
equality' → == rel equality'|!= rel equality'|ε
join' → and equality join'|ε
type' → [ num ] type'|ε
decls → decls'
basic → int | char | float | String
stmt → loc = bool ;|break ;|do stmt while ( bool ) ;|while ( bool ) stmt|if ( bool ) stmt stmt_if(bool)stmt|bolck
